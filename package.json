{
  "name": "floway",
  "private": true,
  "workspaces": ["apps/web", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace @floway/web",
    "build": "npm run build --workspace @floway/web",
    "start": "npm run start --workspace @floway/web",
    "test": "node --test packages/algorithms/*.test.mjs",
    "lint": "echo 'Add ESLint after Expo bootstrap'"
  }
}
